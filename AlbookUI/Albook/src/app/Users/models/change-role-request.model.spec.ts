import { ChangeRoleRequest } from './change-role-request.model';

describe('ChangeRoleRequest', () => {
  it('should create an instance', () => {
    expect(new ChangeRoleRequest()).toBeTruthy();
  });
});
