import { ReqisterRequest } from './reqister-request.model';

describe('ReqisterRequest', () => {
  it('should create an instance', () => {
    expect(new ReqisterRequest()).toBeTruthy();
  });
});
